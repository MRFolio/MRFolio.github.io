import { LocationWeather } from '../components';

const LocationDetails = () => {
  return (
    <main className="main">
      <LocationWeather />
    </main>
  );
};

export default LocationDetails;
